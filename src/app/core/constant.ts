import { environment } from "../../environments/environment";

export const URLS = {
    api: environment.api,
}

export const ACCEPTED_PICTURE_FORMATS = ['image/jpeg', 'image/png', 'image/bmp', 'image/webp']
export const DEFAULT_LANG = 'en'
